
/* 

HTML DOM, JS can access and change all the elements of an HTML document.
When a web page is loaded, the browser creates a Document Object Model (DOM)of the page.
         Document
            Root Elemeent <html>
                Element <head>
                Element <body>
                Element <title>   attribute "href"   Element <a>           Element <h1>
                Text "My Title"                      Element "My Link"     Element "My header"


 * change all the HTML elements in the page
 * change all the HTML elements in the page 
 * change all the CSS styles in the page
 * remove existing HTML elements and attributes 
 * add new HTML elements and attributes 
 * react to all existing HTML events in the page
 * can create new HTML events in the page          

 DOM?  standard for accessing documents:
 The HTML DOM is a standard object model and programming interface for HTML. 
     It defines:
          The HTML elements as objects
          The properties of all HTML elements
          The methods to access all HTML elements
          The events for all HTML elements

    NB:The HTML DOM is a standard for how to get, change, add, or delete HTML elements. 
    
    HTML DOM Methods

      Methods ?
            methods are actions you can perform (html element)
      Properties ?
            are values (of HTML Elements) that you can set or change.

 The DOM Programming Interface 
       HTML DOM can be accessed with JavaScript (and with other programming languages). 
       DOM, all HTML elements are defined as objects.

 The programming interface  consists of : 
          properties
          method
          objects

     A property is a value that you can get or set 
     A method is an action you can do (like add or deleting an HTML element).     
                

*/





